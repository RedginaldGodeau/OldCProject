#include <stdlib.h>
#include <stdio.h> 

int main(int argc, char const *argv[]) { 
    
    int age; // voici une "variable" une variable est une petit boite ou on peut y mettre un element genre un nombre, lettre, ... mais il faut préciser au programme comment faire
            // le "int" indique au programme que la "variable" age est un nombre si on voulais mettre une lettre se serai "char".

    scanf("%i",age); // "scanf" est une instruction qui permet de récupéré se que la personne tape sur son clavier et la stoque dans la "variable" "age" et il faut préciser se que l'on récupere (nombre, lettre,...) donc on met un %
                    //  + la premiere lettre du type (si on veut un nombre on prendra la premiere lettre de int "%i").




    return 0;
} 
 